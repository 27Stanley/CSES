const numberSpiral = require("./numberSpiral");

describe("number spiral problem", () => {
  test("if x and y are equal", () => {
    const input = (5, 5);
    const ouptut = 21;
    expect(numberSpiral(input)).toBe(output);
  });
});
